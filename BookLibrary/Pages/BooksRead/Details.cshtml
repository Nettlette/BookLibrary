@page
@model BookLibrary.Pages.BooksRead.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>BooksRead</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.BooksRead.StartDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.BooksRead.StartDate)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.BooksRead.EndDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.BooksRead.EndDate)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.BooksRead?.BooksReadId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
